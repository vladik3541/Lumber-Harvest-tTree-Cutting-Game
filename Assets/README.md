# Система Прокачки (Lumber Harvest Style)

Система апгрейдів з тригерною зоною, UI панеллю та збереженням прогресу.

## 📋 Компоненти

### Основні скрипти:
- **UpgradeData.cs** - ScriptableObject для даних апгрейду
- **PlayerUpgradeManager.cs** - Менеджер прогресу (Singleton)
- **UpgradeZone.cs** - Тригерна зона для активації UI
- **UpgradeUIPanel.cs** - Контролер панелі апгрейдів
- **UpgradeButton.cs** - Кнопка окремого апгрейду
- **PlayerDamageSystem.cs** - Приклад використання апгрейдів

## 🚀 Швидкий старт

### 1. Створення ScriptableObjects (Дані апгрейдів)

1. ПКМ в Project → Create → Upgrades → Upgrade Data
2. Створи 3 файли:
   - `DamagePerSecond_Upgrade`
   - `HitInterval_Upgrade`
   - `SawCount_Upgrade`

#### Приклад налаштування DamagePerSecond:
```
Upgrade Name: "Урон за секунду"
Description: "Збільшує шкоду яку ти наносиш деревам"
Levels:
  Level 0: value=10, cost=0    (початкове значення)
  Level 1: value=15, cost=100
  Level 2: value=22, cost=200
  Level 3: value=30, cost=400
  Level 4: value=40, cost=800
```

#### Приклад налаштування HitInterval:
```
Upgrade Name: "Швидкість атаки"
Description: "Зменшує час між ударами"
Levels:
  Level 0: value=1.0, cost=0   (1 секунда)
  Level 1: value=0.8, cost=150
  Level 2: value=0.6, cost=300
  Level 3: value=0.4, cost=600
  Level 4: value=0.2, cost=1200
```

#### Приклад налаштування SawCount:
```
Upgrade Name: "Тип пилки"
Description: "Покращує твою пилку на більш ефективну"
Is Saw Upgrade: TRUE (✓) - ВАЖЛИВО!
Levels:
  Level 0: sawPrefab=[BasicSaw], cost=0, description="Стара пилка"
  Level 1: sawPrefab=[BronzeSaw], cost=200, description="Бронзова пилка"
  Level 2: sawPrefab=[SilverSaw], cost=500, description="Срібна пилка"
  Level 3: sawPrefab=[GoldSaw], cost=1250, description="Золота пилка"
  Level 4: sawPrefab=[PlatinumSaw], cost=3125, description="Платинова пилка"
```

**ВАЖЛИВО:** Дляsaw апгрейду галочка "Is Saw Upgrade" має бути ✓ (true)!

### 2. Створення PlayerUpgradeManager

1. Створи порожній GameObject → назви "PlayerUpgradeManager"
2. Додай скрипт `PlayerUpgradeManager.cs`
3. Перетягни створені ScriptableObjects в поля:
   - Damage Per Second Upgrade
   - Hit Interval Upgrade
   - Saw Count Upgrade
4. Встанови стартові гроші (Player Money = 1000)

### 3. Створення UI Панелі

#### Структура Canvas:
```
Canvas
└── UpgradePanel
    ├── Background (Image - темний фон з прозорістю)
    ├── MoneyDisplay (TextMeshPro - "$1000")
    ├── UpgradeButtonsContainer
    │   ├── DamageButton (UpgradeButton)
    │   ├── IntervalButton (UpgradeButton)
    │   └── SawCountButton (UpgradeButton)
    └── CloseButton (опціонально)
```

#### Налаштування UpgradePanel:
1. Додай компонент `UpgradeUIPanel.cs`
2. Налаштуй:
   - Panel Root = UpgradePanel GameObject
   - Canvas Group = додай компонент CanvasGroup
   - Money Text = посилання на TextMeshPro з грошима
   - Fade Speed = 5
   - Use Animation = true

#### Налаштування кожної кнопки (приклад для DamageButton):

Структура кнопки:
```
DamageButton (Button + UpgradeButton.cs)
├── Icon (Image)
├── TitleText (TextMeshPro - "Урон за секунду")
├── LevelText (TextMeshPro - "Рівень 1")
├── ValueText (TextMeshPro - "Урон: 10/сек")
├── CostText (TextMeshPro - "$100")
└── MaxLevelIndicator (GameObject з Image - відображається на MAX)
```

Налаштування UpgradeButton:
- Button = посилання на Button компонент
- Title Text, Level Text, Value Text, Cost Text = відповідні TextMeshPro
- Icon = Image для іконки
- Max Level Indicator = GameObject що показується при максимальному рівні
- Upgrade Type = DamagePerSecond / HitInterval / SawCount
- Affordable Color = білий (можна купити)
- Unaffordable Color = сірий (не вистачає грошей)
- Max Level Color = золотий

### 4. Створення Зони Апгрейдів

1. Створи 3D Object → Cube (або іншу форму)
2. Назви "UpgradeZone"
3. Додай компонент `UpgradeZone.cs`
4. Налаштуй:
   - Box Collider → Is Trigger = true
   - Upgrade Panel = посилання на UpgradeUIPanel
   - Player Tag = "Player"
5. Налаштуй розмір зони (Scale)
6. (Опціонально) Вимкни Mesh Renderer якщо не хочеш бачити куб

### 5. Налаштування гравця

1. Переконайся що на гравці є тег "Player"
2. Додай скрипт `PlayerDamageSystem.cs`
3. Налаштуй:
   - Saw Spawn Point = Transform де буде з'являтись пилка (дочірній об'єкт гравця)
   - Detection Radius = радіус пошуку дерев (3)
   - Tree Layer = Layer для дерев

**ВАЖЛИВО:**Saw Prefab більше не потрібен в PlayerDamageSystem - тепер пилка береться з ScriptableObject!

### 6. Створення префабів пилок

Для кожного рівня апгрейду створи окремий префаб пилки:

1. **BasicSaw (Рівень 0)**
   - Створи 3D модель (Cube або Cylinder)
   - Додай матеріал (сірий колір)
   - Налаштуй розмір (наприклад 0.3x0.3x0.1)
   - Зроби префаб

2. **BronzeSaw (Рівень 1)**
   - Більша модель (0.4x0.4x0.15)
   - Бронзовий матеріал (коричневий/помаранчевий)
   - Можна додати emission для блиску

3. **SilverSaw, GoldSaw, тощо**
   - Кожна пилка більша і красивіша
   - Різні кольори матеріалів
   - Можна додати партикли для високих рівнів

4. Додай всі префаби в ScriptableObject `SawCount_Upgrade`
   - Відкрий SawCount_Upgrade
   - В кожен Level → Saw Prefab перетягни відповідний префаб

### 6. Створення дерев

1. Створи GameObject з 3D моделлю дерева
2. Додай компонент `TreeHealth.cs`
3. Налаштуй:
   - Max Health = 100
   - Money Reward = 10 (скільки грошей дає дерево)
4. Додай Collider
5. Встанови Layer = той самий що в Detection дерев

## 🎮 Як це працює

1. **Гравець заїжджає в зону** → UpgradeZone викликає ShowPanel()
2. **Панель з'являється** → показуються кнопки апгрейдів
3. **Гравець натискає кнопку** → PlayerUpgradeManager перевіряє гроші
4. **Успішна прокачка** → рівень збільшується, гроші списуються
5. **UI оновлюється** → всі кнопки оновлюють свій стан
6. **Зміни застосовуються:**
   - Damage/Interval → PlayerDamageSystem використовує нові значення
   - **Saw → Старий GameObject знищується, створюється новий з іншим префабом!**
7. **Прогрес зберігається** → PlayerPrefs автоматично

### Як працює зміна пилки:
```
Рівень 0 → BasicSaw_Instance
         ↓ (апгрейд)
         Destroy(BasicSaw_Instance)
         ↓
Рівень 1 → Instantiate(BronzeSaw)
         ↓ (апгрейд)
         Destroy(BronzeSaw_Instance)
         ↓
Рівень 2 → Instantiate(SilverSaw)
```

## 📊 Формули балансу

### Лінійне зростання:
```csharp
damage[i] = baseDamage + (i * increment)
// Приклад: 10, 15, 20, 25, 30
```

### Експоненційне зростання:
```csharp
damage[i] = baseDamage * Mathf.Pow(multiplier, i)
// Приклад: 10, 15, 22.5, 33.75, 50.6
```

### Вартість з експоненційним зростанням:
```csharp
cost[i] = baseCost * Mathf.Pow(costMultiplier, i)
// Приклад: 100, 200, 400, 800, 1600
```

## 🔧 Додаткові функції

### Скидання прогресу (для тестування):
```csharp
PlayerUpgradeManager.Instance.ResetUpgrades();
```

### Додавання грошей (для тестування):
```csharp
PlayerUpgradeManager.Instance.AddMoney(1000);
```

### Отримання поточних значень:
```csharp
float damage = PlayerUpgradeManager.Instance.CurrentDamagePerSecond;
float interval = PlayerUpgradeManager.Instance.CurrentHitInterval;
GameObject sawPrefab = PlayerUpgradeManager.Instance.CurrentSawPrefab; // GameObject!
```

### Різниця між типами апгрейдів:
```csharp
// Damage та Interval - прості значення (float)
damagePerSecondUpgrade.isSawUpgrade = false;
float value = upgradeData.GetValue(level); // Працює

// Saw - GameObject префаби
sawCountUpgrade.isSawUpgrade = true; // ВАЖЛИВО!
GameObject prefab = upgradeData.GetSawPrefab(level); // Використовуй це
float value = upgradeData.GetValue(level); // НЕ використовуй для пилок!
```

## 🎨 Візуальні покращення

### Анімації кнопок:
- Додай DOTween для smooth transitions
- Scale pulse при наведенні
- Color tween при зміні стану

### Партикли:
- Ефект при успішному апгрейді
- Sparkles навколо кнопки яку можна купити
- Money particles при зароблянні грошей

### Звуки:
- Клік на кнопку
- Успішна прокачка
- Недостатньо грошей (error sound)
- Money collect sound

## 🐛 Debug режим

Увімкни `Show Debug Messages` в UpgradeZone для логів:
- "Гравець увійшов в зону апгрейдів"
- "Гравець вийшов з зони апгрейдів"
- "Прокачано [тип]!"
- "Дерево знищено! Отримано $X"

## 📝 Чек-лист

- [ ] Створено 3 ScriptableObject для апгрейдів
- [ ] В SawCount_Upgrade встановлено `Is Saw Upgrade = true`
- [ ] Створено префаби пилок (BasicSaw, BronzeSaw, SilverSaw, тощо)
- [ ] Всі префаби додано в SawCount_Upgrade levels
- [ ] Створено PlayerUpgradeManager на сцені
- [ ] Створено Canvas з UpgradePanel
- [ ] Налаштовано 3 кнопки апгрейдів
- [ ] Створено UpgradeZone з тригером
- [ ] Гравець має тег "Player"
- [ ] На гравця додано PlayerDamageSystem
- [ ] Створено Saw Spawn Point (дочірній Transform)
- [ ] Дерева мають TreeHealth компонент
- [ ] Протестовано заїзд в зону
- [ ] Протестовано прокачку damage/interval
- [ ] Протестовано зміну пилки (GameObject swap)
- [ ] Перевірено збереження прогресу

## 🚨 Поширені помилки

1. **Панель не з'являється** → перевір Is Trigger на зоні, тег гравця
2. **Кнопки не працюють** → перевір посилання на PlayerUpgradeManager
3. **Не зберігається прогрес** → перевір що Manager не знищується між сценами
4. **Пилка не змінюється** → перевір `Is Saw Upgrade = true` в ScriptableObject
5. **Пилка не з'являється** → перевір що префаби додано в levels
6. **UI не оновлюється** → перевір події OnMoneyChanged та OnUpgradeChanged
7. **Стара пилка залишається** → перевір OnUpgradesChanged в PlayerDamageSystem
8. **NullReferenceException на пилці** → додай всі префаби в SawCount_Upgrade

Готово! Тепер у тебе є повноцінна система апгрейдів як у Lumber Harvest! 🎮

### Ключова різниця:
- **Damage та Interval** - змінюються числові значення
- **Saw** - змінюється сам GameObject (префаб)
